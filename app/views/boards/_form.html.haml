= form_for @board do |f|
  .board_title 
    = f.label :title, class: 'control_label'
    = f.text_field :title, class: 'text'
  .board_content
    = f.label :content, class: 'control_label'
    = f.text_area :content, class: 'content'
  .actions
    = f.submit "Submit", class: 'submit'